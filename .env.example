# Persona Guard reads credentials from the inherited process environment.
# scripts/run-server intentionally does not load this file.

# Standard setup:
DEEPSEEK_API_KEY=

# Optional compatibility name.  When both are set, the backend checks this
# variable first and falls back to DEEPSEEK_API_KEY.
# GMEM_DEEPSEEK_API_KEY=
